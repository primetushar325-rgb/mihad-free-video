"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {Home,Flame,Smartphone,Youtube,Newspaper} from "lucide-react";
const items=[['/','Home',Home],['/trending','Trending',Flame],['/apps','Premium Apps',Smartphone],['/youtube','YouTube',Youtube],['/news','News',Newspaper]] as const;
export default function BottomNavigation(){const p=usePathname();return <nav aria-label="Primary mobile navigation" className="glass-strong fixed inset-x-2 bottom-[calc(.5rem+env(safe-area-inset-bottom))] z-50 mx-auto flex max-w-md items-center justify-around rounded-[24px] border border-gold-500/25 px-1 py-1.5 shadow-2xl md:hidden">{items.map(([href,label,Icon])=>{const active=href==='/'?p===href:p.startsWith(href);return <Link key={href} href={href} className={`flex min-w-0 flex-1 flex-col items-center gap-0.5 rounded-2xl px-1 py-2 text-[9px] font-semibold transition ${active?'bg-gold-gradient text-black shadow-gold':'text-neutral-400'}`}><Icon className="h-5 w-5" fill={active&&label==='YouTube'?'currentColor':'none'}/><span className="truncate">{label}</span></Link>})}</nav>}
