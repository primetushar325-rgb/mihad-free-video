"use client";import Image from'next/image';import{Calendar,ExternalLink,Pin,Share2,Youtube}from'lucide-react';import type{NewsPost}from'@/types';function ytId(url:string){try{const u=new URL(url);if(u.hostname.includes('youtu.be'))return u.pathname.slice(1);if(u.hostname.includes('youtube.com'))return u.searchParams.get('v')||u.pathname.split('/').filter(Boolean).pop()||''}catch{}return''}export default function NewsCard({post}:{post:NewsPost}){const image=post.imageUrl||(ytId(post.youtubeUrl)?`https://i.ytimg.com/vi/${ytId(post.youtubeUrl)}/hqdefault.jpg`:'');async function share(){const data={title:post.title,text:post.body.slice(0,120),url:location.href};if(navigator.share)await navigator.share(data);else await navigator.clipboard.writeText(location.href)}return <article className="overflow-hidden rounded-3xl border border-white/8 bg-neutral-950/80 shadow-glass"> <div className="flex items-center gap-3 p-4"><span className="flex h-10 w-10 items-center justify-center rounded-full bg-gold-gradient font-black text-black">M</span><div className="min-w-0 flex-1"><p className="font-bold text-white">Mihad Free Video</p><p className="flex items-center gap-1 text-[11px] text-neutral-500"><Calendar className="h-3 w-3"/>{new Date(post.publishDate).toLocaleString()}</p></div>{post.pinned&&<span className="badge bg-gold-500/15 text-gold-300"><Pin className="h-3 w-3"/>Pinned</span>}</div><div className="px-4 pb-4"><span className="text-xs font-semibold text-gold-300">{post.topic||'Latest News'}</span><h2 className="mt-1 font-display text-xl font-bold text-white">{post.title}</h2><p className="mt-2 whitespace-pre-line text-sm leading-relaxed text-neutral-300">{post.body}</p></div>{image&&<div className="relative aspect-video w-full bg-white/5"><Image src={image} alt={post.title} fill sizes="(max-width:768px) 100vw,700px" className="object-cover"/></div>}<div className="flex flex-wrap gap-2 p-4">{post.youtubeUrl&&<a href={post.youtubeUrl} target="_blank" rel="noopener noreferrer" className="inline-flex flex-1 items-center justify-center gap-2 rounded-xl bg-red-600 px-3 py-2.5 text-xs font-bold text-white"><Youtube className="h-4 w-4"/>Watch on YouTube</a>}{post.externalUrl&&<a href={post.externalUrl} target="_blank" rel="noopener noreferrer" className="btn-ghost flex-1 px-3 py-2.5 text-xs"><ExternalLink className="h-4 w-4"/>View</a>}<button onClick={share} className="btn-ghost px-3 py-2.5" aria-label="Share post"><Share2 className="h-4 w-4"/></button></div></article>}
