import type{YouTubeChannel}from'@/types';import Image from'next/image';import{Youtube,ExternalLink}from'lucide-react';import{listYouTubeChannels}from'@/lib/repository';export const revalidate=60;export default async function Page(){let channels:YouTubeChannel[]=[];try{channels=await listYouTubeChannels()}catch{}return <section className="mx-auto max-w-5xl px-3 py-6 sm:px-5"><h1 className="font-display text-2xl font-black text-white">▶️ YouTube</h1><p className="mb-5 text-sm text-neutral-400">Channels and useful YouTube updates.</p>{!channels.length?<div className="card text-center text-neutral-400">Channels will appear here when published.</div>:<div className="grid gap-3 sm:grid-cols-2">{channels.map(c=><article key={c.id} className="card flex gap-4"><div className="relative h-16 w-16 shrink-0 overflow-hidden rounded-full bg-red-500/15">{c.logoUrl?<Image src={c.logoUrl} alt="" fill sizes="64px" className="object-cover"/>:<Youtube className="m-4 h-8 w-8 text-red-500"/>}</div><div><h2 className="font-bold text-white">{c.name}</h2><p className="mt-1 text-xs text-neutral-400">{c.description}</p><a href={c.youtubeUrl} target="_blank" rel="noopener noreferrer" className="mt-3 inline-flex items-center gap-1.5 text-sm font-bold text-red-400">Open Channel <ExternalLink className="h-4 w-4"/></a></div></article>)}</div>}</section>}
