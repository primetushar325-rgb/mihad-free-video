import type{VideoWithCategory}from'@/types';import TrendingBrowser from '@/components/site/TrendingBrowser';import {listTrendingVideos} from '@/lib/repository';import {isDbConfigured}from'@/lib/db';export const revalidate=60;export default async function Page(){let videos:VideoWithCategory[]=[];try{if(isDbConfigured())videos=await listTrendingVideos()}catch{}return <section className="mx-auto max-w-7xl px-3 py-6 sm:px-5"><TrendingBrowser videos={videos}/></section>}
