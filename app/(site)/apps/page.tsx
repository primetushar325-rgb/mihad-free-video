import type{PremiumApp}from'@/types';import Image from'next/image';import{Download,ExternalLink,Smartphone}from'lucide-react';import{listPremiumApps}from'@/lib/repository';export const revalidate=60;export default async function Page(){let apps:PremiumApp[]=[];try{apps=await listPremiumApps()}catch{}return <section className="mx-auto max-w-5xl px-3 py-6 sm:px-5"><h1 className="font-display text-2xl font-black text-white">📱 Premium Apps</h1><p className="mb-5 text-sm text-neutral-400">Useful apps, ordered by the admin.</p>{!apps.length?<Empty/>:<div className="grid gap-3 sm:grid-cols-2">{apps.map((a,i)=><article key={a.id} className="card flex gap-3"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-gold-gradient text-xs font-black text-black">{i+1}</span><div className="relative h-16 w-16 shrink-0 overflow-hidden rounded-2xl bg-white/5">{a.iconUrl?<Image src={a.iconUrl} alt="" fill sizes="64px" className="object-cover"/>:<Smartphone className="m-4 h-8 w-8 text-gold-300"/>}</div><div className="min-w-0 flex-1"><h2 className="font-bold text-white">{a.name}</h2>{a.version&&<p className="text-[11px] text-gold-300">Version {a.version}</p>}<p className="mt-1 line-clamp-2 text-xs text-neutral-400">{a.description}</p><div className="mt-3 flex gap-2"><a href={a.apkUrl} className="btn-gold px-3 py-2 text-xs" download><Download className="h-3.5 w-3.5"/>Download</a>{a.pageUrl&&<a href={a.pageUrl} target="_blank" rel="noopener noreferrer" className="btn-ghost px-3 py-2 text-xs"><ExternalLink className="h-3.5 w-3.5"/>Details</a>}</div></div></article>)}</div>}</section>};function Empty(){return <div className="card text-center text-neutral-400">Apps will appear here when the admin publishes them.</div>}
